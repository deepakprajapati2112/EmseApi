"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const simple_1 = __importDefault(require("@ovyerus/licenses/simple"));
const _1 = require(".");
function config({ name, license, email }) {
    if (!name && !license && !email) {
        if (_1.cfg.get("name"))
            console.log(`name=${_1.cfg.get("name")}`);
        if (_1.cfg.get("license"))
            console.log(`license=${_1.cfg.get("license")}`);
        if (_1.cfg.get("email"))
            console.log(`email=${_1.cfg.get("email")}`);
    }
    if (license) {
        if (!simple_1.default.has(license))
            throw new Error("license must be a valid SPDX identifier");
        _1.cfg.set({ license });
    }
    if (name)
        _1.cfg.set({ name });
    if (email)
        _1.cfg.set({ email });
}
exports.default = config;
//# sourceMappingURL=config.js.map