import { Identifiers } from "@ovyerus/licenses";
interface RunArgv {
    license?: Identifiers[number];
    name: string;
    email: string;
    year: string;
    raw?: boolean;
    nonOsi?: boolean;
    project: ReturnType<typeof import("./utils").convertProject>;
    projectName?: string;
}
export default function run({ license: license_, name: author, email, year, raw, nonOsi, project, projectName }: RunArgv): Promise<void>;
export {};
